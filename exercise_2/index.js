const PORT = 3001
const express = require("express")
const app = express()

// Write your routes between the ***

// ***

// ***

app.listen(PORT, () => {
    console.log(`Listening on port ${PORT}`)
})